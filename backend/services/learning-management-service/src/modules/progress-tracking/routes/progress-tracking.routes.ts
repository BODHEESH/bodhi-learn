import { Router } from 'express';
import { ProgressTrackingController } from '../controllers/progress-tracking.controller';

const router = Router();
const controller = new ProgressTrackingController();

// Routes will be implemented

export default router;
