{
  "name": "bodhi1",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "api-gateway",
    "service-registry",
    "services/*",
    "client"
  ],
  "scripts": {
    "dev": "docker-compose -f docker-compose.dev.yml up",
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write ."
  }
}
